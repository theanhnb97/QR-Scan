# Internal Changelog

This is an implementation/project changelog, not store marketing copy.

## Unreleased

### Added
- Initial product, architecture, UI/UX, security, QA, memory, and handoff specification pack.
- Supplied QR Scan brand assets wired into the React Native shell through `src/ui/assets.ts`.

### Changed
- Replaced generic shell colors with the approved blue/navy/mint theme tokens.
- Added logo lockups and the `Scan from Image` secondary action to the Scan permission state.
- Replaced emoji placeholders in History, Auth, and Create empty states with the branded mark.
- Replaced the default iOS bundle identifier with `com.anhnt.qrscan` for Apple registration.
- Synchronized the Android namespace, application ID, and native package with `com.anhnt.qrscan`.
- Replaced the Xcode-incompatible VisionCamera integration with CameraKit 16.2.1 for live scanning and ML Kit barcode scanning for image imports.
- Added camera permission setup, real-time scan haptics, and successful iOS/Android native build verification.
- Replaced mock app-container repositories with SQLite-backed history/authenticator metadata repositories and native Keychain/Keystore credential storage.
- Added typed result actions, URL in-app WebView, scanner flash control, history search/favorites/delete/clear, TOTP account UI, biometric reveal/export, and QR/Code 128 generation.
- Added `react-native-keychain`, `@op-engineering/op-sqlite`, `react-native-svg`, `react-native-qrcode-svg`, `react-native-view-shot`, CameraRoll, Clipboard, and WebView with native build verification.
- Added app-lock/settings lifecycle protection, configurable URL behavior, safe CSV/JSON history export, authenticator metadata editing, and native Wi-Fi/contact/calendar action adapters.
- Added supplied launcher icon assets to iOS and Android platform resource catalogs.
- Corrected Wi-Fi payload tokenization so escaped SSIDs/passwords generated by QR creation round-trip through the parser.
- Added the matching React Native asset registry dependency required for `react-native-svg` device bundling.
- Added Face ID usage text, add-only iOS Photos permission handling, reduced Android storage permission scope, and accessibility state/labels across core controls.
- Added environment/file-based Android release signing with an explicit unsigned fallback when no production keystore is configured.
- Added Generate-tab keyboard dismissal controls, iOS input accessory “Done” action, scroll/keyboard avoidance, and a direct “Back to Scan” navigation action.
- Added Vietnamese-first localization with an English switch, header back actions, swipe-to-action rows, flash on/off icons, CameraKit zoom presets, scan-result copy, Authenticator countdown rings/copy feedback, and native image clipboard support.
- Replaced the Scan flash glyph with a lightning bolt, added a draggable camera zoom slider, renamed image import to Choose from library, clarified the Auth tab with a key icon, and added capped generated-code history with conditional actions.
- Replaced the Auth tab key glyph with a circular TOTP timer icon, moved Create history into the header beside Settings, and made bottom sheets dismissible by tapping outside the sheet.
- Standardized functional controls on the shared `AppIcon` SVG set, replacing the remaining favorite glyph and adding balanced icon-plus-label swipe actions and authenticator buttons.
- Added QR Scan title/tagline to the native iOS launch screen, fixed keyboard avoidance and iOS accessory dismissal for Authenticator forms, exposed TOTP secure actions through a visible options button, and removed the extra Create history sheet wrapper that caused bottom spacing.
- Replaced the screenshot-based splash with native iOS/Android splash resources using the supplied centered mark and light/dark backgrounds.
- Replaced iOS full-bleed and Android adaptive launcher resources with the supplied platform sources.
- Completed the manifest-driven icon audit and corrected Authenticator swipe direction to right-to-left with right-side Edit/Delete actions.
- Stabilized the draggable Scan zoom slider after repeated tab switches, removed the secondary Authenticator time/copy hint, and added a near-expiry danger-color transition for TOTP codes.
- Added Google Authenticator migration QR decoding with a reviewable account list, metadata duplicate detection, and secure confirmed bulk import.
- Added Google Authenticator import shortcuts to the empty Authenticator state and Settings, with scanner guidance for the cross-device QR export flow.
- Compacted Authenticator rows into two lines with right-aligned countdown time and added account search.
- Added bulk export to Google Authenticator migration QR format from the Authenticator list, search-bar action, and Settings. Export requires confirmation plus biometric/device authentication, supports multiple QR batches, and keeps the compact circular countdown indicator in each TOTP row.
- Moved Authenticator Add account to the top-right plus action with setup-key and QR-code options, removing the fixed bottom action bar.
- Sanitized the publish history to a single root `Init` commit and removed the tracked debug keystore from the repository tree.

### Fixed
- Updated Jest setup so the app-root smoke test does not execute React Navigation ESM internals.
- Corrected TOTP implementation so the selected SHA256/SHA512 algorithm is actually used instead of silently falling back to SHA1.
- Added unit coverage for stable zoom gesture math and TOTP countdown color interpolation.
- Added migration parser and history-sanitization tests proving imported secrets stay out of normal history.
- Fixed long scan-result overflow hiding actions and corrected QR format recognition so Google Authenticator migration scans reach the import action.
- Added migration export round-trip and QR batch-splitting unit tests.

### Security
- TOTP secret storage and redaction requirements documented.
- Added tests proving authenticator secrets do not enter SQL statements or history persistence.
- Added native biometric gate before reveal/export of authenticator secrets.
- Verified a signed iOS Debug build installs and launches on the connected iPhone with bundle ID `com.anhnt.qrscan`.
- Verified the final signed iOS Release artifact installs and launches on the connected iPhone with bundle ID `com.anhnt.qrscan` after the Generate-tab UX fixes.
- Verified iOS Simulator Debug, Android Debug, and the final signed iOS Release device build after the UI enhancement pass.
- Verified the native splash/launcher change set with iOS Simulator Debug and Android Debug builds on 2026-09-20.
- Re-ran the final keyboard/splash/sheet validation on 2026-09-20; TypeScript, 27 Jest tests, lint (0 errors), iOS Simulator Debug, and Android Debug all passed.
- Re-exported all iOS AppIcon scale slots from the full-bleed master to remove the baked white border that iOS was masking a second time.
- Verified an arm64-v8a Android Release build after regenerating CameraKit codegen; installed and launched the locally debug-signed APK on the connected `Active_3` device.
- Verified the arm64-v8a Android Release build after bulk export changes; installed and launched the locally debug-signed APK on the connected `Active_3` device.
- Fixed Android scan result crashes: declared `android.permission.VIBRATE`, guarded haptic feedback, presented results before history writes finish, and restricted CameraKit barcode formats to Android-supported values.
- Added a theme-aware transparent app mark to the left side of the Scan header and removed the dark-logo opaque background. Verified with a Debug APK on the connected Android device.
- Documented the wireless Android debug requirement to map Metro through `adb reverse tcp:8081 tcp:8081` before launching Debug.
- Changed the default URL action to the external browser and removed the duplicate external-browser action from URL result sheets; saved preferences remain authoritative.
- Fixed the Android scanner path by disabling CameraKit's JS format allow-list, preventing valid QR detections from being filtered before the React Native callback.
- Fixed Android Authenticator countdown drift by caching secure secrets for the active account list and preventing overlapping Keystore reads on each timer tick.
- Built and installed the current arm64-v8a Android Release candidate on wireless `Active_3` using a locally debug-signed APK; production signing remains pending.
